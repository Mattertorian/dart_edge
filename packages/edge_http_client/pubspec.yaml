name: edge_http_client
description: A http package implementation for Dart Edge.
homepage: https://dartedge.dev
# repository: https://github.com/invertase/dart_edge/tree/main/packages/edge_http_client
publish_to: none
version: 0.0.1+3

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  # edge_runtime: ^0.0.4+2
  edge_runtime: 
    git: 
      url: https://github.com/Mattertorian/dart_edge.git
      path: packages/edge_runtime
  # edge_runtime: 
  #   path: ../edge_runtime
  http: ^0.13.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
